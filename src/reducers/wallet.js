const INITIAL_STATE = {
  wallet: {
    currencies: [],
    expenses: [],
  },
};

const stateWallet = (state = INITIAL_STATE, action) => {
  switch (action.type) {
  default:
    return state;
  }
};

export default stateWallet;
